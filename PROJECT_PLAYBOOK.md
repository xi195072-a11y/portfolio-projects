# Project Playbook — 新项目启动章程

> **每次开始新项目前，AI 必须先读这份文件，然后一步步带你走完整个流程。**
> 你只需要说"我要做一个新项目"，剩下的按这个章程来。

---

## 总览：五步走

```
第一步：可行性分析 → 第二步：写设计文档 → 第三步：出原型 → 第四步：AI写代码 → 第五步：测试验收
         ↑                                                                          ↓
         └──────────── 如果测试不通过，回到第四步让AI修 ←──────────────────────────────┘
```

核心理念：**你负责"想清楚"和"验收"，AI负责"干活"。**

---

## 第一步：可行性分析（30分钟）

> 目标：搞清楚这个项目值不值得做、能不能做。

AI 需要问你以下问题，用大白话回答就行：

### 1.1 这个项目要解决什么问题？
- 用一句话说清楚
- 例："帮小公司自动分析竞品社交媒体数据，生成一份简单的报告"

### 1.2 谁会用它？
- 想象一个具体的人
- 例："一个不懂技术的运营人员"

### 1.3 技术上能不能做？
- AI 帮你评估：需要什么技术？难不难？有没有现成的参考？
- 有没有类似的开源项目可以学习？（参考 PROJECT_BRIEF.md 第7节）

### 1.4 对我的求职有没有帮助？
- 能展示什么技能？
- 跟目标岗位（Data Analyst / AI Engineer / Tech Ops）有没有关系？

### 1.5 结论
- **做** → 进入第二步
- **不做** → 换一个想法，重新来

---

## 第二步：写设计文档（1小时）

> 目标：把要做的东西想清楚、写下来，变成 AI 能理解的格式。

AI 会帮你生成一份设计文档，保存在项目文件夹里（`DESIGN.md`）。内容如下：

### 2.1 一句话描述
> 这个项目是什么？给谁用的？解决什么问题？

### 2.2 用户场景
> 描述一个具体的使用场景，像讲故事一样：
> "小明打开网页 → 上传了一个Excel文件 → 点了一下按钮 → 看到了一张图表和一段分析文字"

### 2.3 功能清单（分优先级）
> - **P0（必须有）**：没有这些功能就不能用
> - **P1（最好有）**：有了更好，但没有也行
> - **P2（锦上添花）**：以后再说

### 2.4 技术方案
> AI 根据你的需求，推荐：
> - 用什么框架？（Gradio / Streamlit / CLI）
> - 需要什么依赖包？
> - 数据怎么存？
> - API 怎么调？

### 2.5 最简版（MVP）定义
> 第一版只做 P0 功能，能跑起来就行。
> 例："能读一个 CSV 文件，画一张柱状图，写一段总结文字"

### 2.6 验收标准
> 怎么算"做完了"？列出具体条件：
> - [ ] 能正常启动，不报错
> - [ ] 能完成核心功能
> - [ ] 界面能看懂
> - [ ] 能部署到 HuggingFace Spaces

---

## 第三步：出原型（30分钟）

> 目标：先让 AI 画一个界面草图，你看看方向对不对。

### 3.1 AI 生成 UI 代码
- AI 根据设计文档，用 Gradio 写出最简版的界面代码
- 不需要连 API，界面是"假"的也没关系

### 3.2 你来验收
- 跑起来看看：`python app.py`
- 界面布局合不合理？按钮位置对不对？输入框够不够？

### 3.3 反馈修改
- 告诉 AI 哪里要改："这个按钮太大了"、"输入框要加一个下拉选择"
- AI 修改后再给你看
- 重复 2-3 轮，直到你满意

### 3.4 确认原型
- 满意后，进入第四步

---

## 第四步：AI 写代码（按里程碑推进）

> 目标：AI 按设计文档一步步写代码，你不用看每一行，但要跟进进度。

### 4.1 拆分成里程碑
AI 会把开发拆成几个小阶段，每个阶段完成一个具体目标：

```
里程碑1：搭建基础框架（能启动、能显示界面）
里程碑2：接入核心功能（API调用、数据处理）
里程碑3：完善界面（错误提示、加载状态、结果展示）
里程碑4：写 README 和部署文件
```

### 4.2 每个里程碑的流程
1. AI 写完代码
2. 你跑一下：`python app.py`
3. 看看功能对不对
4. 有问题 → 告诉 AI 哪里不对，让它修
5. 没问题 → 进入下一个里程碑

### 4.3 关键规则
- **一次只做一个里程碑**，不要同时搞多个
- **每个里程碑完成后 git commit**，方便回退
- **不要跳过测试**，哪怕只是简单点几下

---

## 第五步：测试验收

> 目标：确认做出来的东西能用、好用、能展示。

### 5.1 功能测试（你来）
- 正常操作一遍：输入 → 点按钮 → 看结果
- 故意输错东西：空值、特殊字符、超长文字 → 看会不会崩溃
- 换不同浏览器/设备试试（如果有的话）

### 5.2 代码检查（AI来）
- AI 检查代码是否符合 PROJECT_BRIEF.md 的规范
- 检查有没有硬编码的 API Key
- 检查 requirements.txt 有没有固定版本

### 5.3 部署测试
- 本地跑没问题后，部署到 HuggingFace Spaces
- 在线版本再测试一遍

### 5.4 写 README
- AI 根据实际代码生成 README
- 你确认截图、描述是否准确

### 5.5 面试准备
- AI 帮你准备面试问题的答案（参考 PROJECT_BRIEF.md 第9节）
- 你至少要能回答：为什么做这个？怎么工作的？最难的部分是什么？

---

## 附录：快速启动模板

当你说"我要做一个新项目"时，AI 会按这个顺序来：

```
1. 读 PROJECT_BRIEF.md（了解技术规范和代码标准）
2. 读这份文件（了解开发流程）
3. 问你：想做什么？（用大白话描述就行）
4. 带你走第一步：可行性分析
5. 通过后，带你走第二步：写设计文档
6. 设计文档确认后，第三步：出原型
7. 原型满意后，第四步：按里程碑写代码
8. 代码写完后，第五步：测试验收
```

**你不需要记住这些步骤，AI 会一步步带你走。你只需要回答问题和验收成果。**

---

## 附录：设计文档模板（DESIGN.md）

```markdown
# [项目名称] — 设计文档

## 一句话描述
[这个项目是什么？给谁用的？解决什么问题？]

## 用户场景
[描述一个具体的使用流程]

## 功能清单
### P0（必须有）
- [ ] 功能1
- [ ] 功能2

### P1（最好有）
- [ ] 功能3

### P2（以后再说）
- [ ] 功能4

## 技术方案
- 框架：[Gradio / Streamlit / CLI]
- 依赖：[列出需要的包]
- 数据存储：[CSV / JSON / SQLite]
- API：[DeepSeek / 其他]

## 最简版（MVP）定义
[第一版只做哪些功能]

## 验收标准
- [ ] 能正常启动，不报错
- [ ] 核心功能可用
- [ ] 界面清晰易懂
- [ ] 可部署到 HuggingFace Spaces
```

---

*Last updated: 2026-08-25*
*这份文件是活的，随着项目经验积累会持续更新。*
